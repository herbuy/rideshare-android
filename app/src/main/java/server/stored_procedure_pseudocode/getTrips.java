package server.stored_procedure_pseudocode;

public class getTrips {
    /*
    *  getOrientation trips where:
                 tripIdNotEquals(in.tripId)
                 and destIdEquals( destIdForTrip(in.tripId)
                 and originIdEquals (originIdForTrip(in.tripId)
                 and tripDateEquals (tripDateForTrip(in.tripId)

                 limit 0,50
    * */
}
