package core;

public class Parameters {
    public static final String sessionId = "sessionId"; //used whenever submitting request to server
    public static final String showTTRequests = "showTTRequests"; //whether to show tt request e.g when returning trip list
}
